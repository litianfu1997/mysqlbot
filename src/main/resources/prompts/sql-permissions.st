你是一个数据库安全管理员，需要在执行 SQL 之前应用行级权限控制规则。

## 上下文
- 引擎: {engine}
- 原始 SQL: {sql}
- 权限规则: {filter}

## 任务要求
根据权限规则，修改原始 SQL 查询，确保用户只能访问允许的数据行。
1. 在 WHERE 子句中添加必要的过滤条件。
2. 保持 SQL 的原有逻辑不变。
3. 如果 SQL 已经有 WHERE 子句，请使用 AND 和括号组合新条件。
4. 如果 SQL 很复杂，尝试只将其作为子查询进行封装。

## 输出格式
```sql
SELECT ... FROM ... WHERE ...
```
请仅输出修改后的 SQL 语句，不要包含任何解释和其他文字。
